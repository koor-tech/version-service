{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  extends: [
    "config:recommended",
    ":automergeMinor",
  ],
  regexManagers: [
    {
      customType: "regex",
      fileMatch: ["^data/.*\\.json"],
      "matchStrings": [
        '".*?": \\{\
\\s*"version": "(?<currentValue>.*?)",\
\\s*"image_uri": "(?<depName>koorinc/koor-operator?):.*?"'
      ],
      autoReplaceStringTemplate: '"{{{newValue}}}": {\n\
            "version": "{{{newValue}}}",\n\
            "image_uri": "{{{depName}}}:{{{newValue}}}"',
      datasourceTemplate: "docker",
    },
    {
      customType: "regex",
      fileMatch: ["^data/.*\\.json"],
      "matchStrings": [
        '".*?": \\{\
\\s*"version": "(?<currentValue>.*?)",\
\\s*"image_uri": "(?<depName>koorinc/ceph?):.*?"'
      ],
      autoReplaceStringTemplate: '"{{{newValue}}}": {\n
            "version": "{{{newValue}}}"\n,
            "image_uri": "{{{depName}}}:{{{newValue}}}"',
      datasourceTemplate: "docker",
    },
  ],
}
